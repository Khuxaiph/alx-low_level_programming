#include <stdio.h>
/*iniialising the code*/
/**
 * main - prints the alphabet in lowercase,
 * followed by a new line, except q and e
 * Return: Aways 0 (Sucess)
 */
int main(void)
{
int low = 'a';
while (low = 'a'; low <= 'z')
{
if (ch != 'e' && != 'q')
{
putchar(low);
}
low++;
}
putchar('\n');
return (0);
}
